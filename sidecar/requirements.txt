# AI MIDI Composer sidecar dependencies.
# Install into a dedicated venv, then PyInstaller-freeze into sidecar.exe.

# -- HTTP server --------------------------------------------------------------
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2

# -- Model inference ----------------------------------------------------------
# NOTE: On CUDA systems, install the matching torch wheel FIRST, outside this
# file, via:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# On CPU-only, the default pypi torch wheel works.
torch>=2.3.0
transformers==4.45.2
huggingface_hub==0.25.1
accelerate==0.34.2
safetensors==0.4.5

# -- MIDI handling ------------------------------------------------------------
mido==1.3.2
# MIDI-LLM detokenizer lives in EleutherAI's anticipation package
anticipation @ git+https://github.com/jthickstun/anticipation.git@main

# -- Misc ---------------------------------------------------------------------
numpy>=1.26
psutil==6.0.0
